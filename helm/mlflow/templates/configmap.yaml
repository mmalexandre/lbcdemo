apiVersion: v1
kind: ConfigMap
metadata:
  name: mlflow-config
  namespace: {{ .Values.namespace }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  AWS_DEFAULT_REGION: {{ .Values.awsRegion | quote }}
